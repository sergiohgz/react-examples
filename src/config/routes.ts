class Routes {
    basePath = '/';
    home = `${this.basePath}home`;
    todos = `${this.basePath}todos`;
    todosHooks = `${this.basePath}todos-hooks`;
}

const routes = new Routes();

export default routes;
